<!--
模板：bizrule（业务规则）
位置：.knowledge/bizrules/{slug}.md
层级：L2（执行编排——业务判断逻辑）
必填段：TL;DR / 规则描述 / 适用条件 / 计算/判断逻辑
质量门禁：计算/判断逻辑必须可被代码实现（含具体公式或分支）

填写指南：
- 一条规则一篇文档，避免在同一篇里塞多个规则
- 计算公式用代码块/伪代码表达，禁止"大约"、"通常"等模糊用词
- 历史变更段记录规则演进，便于追溯老订单按当时规则计算
-->
---
title: {规则名称}                              # 例：小费 T+1 结算规则
type: bizrule
tags: [{3-5 个标签}]
owner: "@{mis-id}"
created: {YYYY-MM-DD}
expires: {YYYY-MM-DD}                          # 默认 180 天
status: draft
sources:
  - "{业务方文档 / 评审纪要}"
related:
  - "[[{关联流程 slug}]]"
  - "[[{关联术语 slug}]]"
---

## TL;DR

{50-100 词，"什么场景 + 怎么判断/计算 + 关键参数"}

## 规则描述

{用业务语言完整描述这条规则。让产品/运营/技术看完都能理解。}

## 适用条件

> 满足以下**全部**条件时本规则生效：

- 条件 1：{业务场景}
- 条件 2：{时间范围 / 地域 / 用户类型}
- 条件 3：{...}

> 以下情况**不**适用本规则：

- 例外 1：{描述}
- 例外 2：{描述}

## 计算 / 判断逻辑

### 输入
| 参数 | 类型 | 来源 | 说明 |
|------|------|------|------|
| `{param}` | int | {表/接口} | {含义} |

### 公式 / 分支

```
# 伪代码或公式
if condition_a:
    result = base_amount * rate_a
elif condition_b:
    result = base_amount * rate_b - deduction
else:
    result = 0

# 关键参数：
#   rate_a = 0.05     # 由 [[xxx]] 决定
#   rate_b = 0.03
#   deduction = 2     # 固定扣除
```

### 输出
| 字段 | 类型 | 说明 |
|------|------|------|
| `{result}` | decimal(10,2) | {含义} |

## 边界场景（可选）

| 场景 | 输入 | 期望输出 | 说明 |
|------|------|---------|------|
| 边界 1 | `amount=0` | `result=0` | 兜底 |
| 边界 2 | `amount` 超大 | `result=upper_limit` | 封顶 |
| 边界 3 | 跨日订单 | 按下单日规则 | 历史兼容 |

## 历史变更（可选）

| 版本 | 生效时间 | 变更点 | 决策来源 |
|------|---------|-------|---------|
| v1 | 2025-01-01 | 初版规则 | [[adr-xxx]] |
| v2 | 2026-03-01 | rate_a 从 0.03 → 0.05 | [[adr-yyy]] |

> 老订单（生效时间前）继续按旧版本规则计算，禁止追溯。

## 关联流程（可选）

- [[{flow-slug}]]：本规则在该流程的第 N 步生效

