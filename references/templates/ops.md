<!--
模板：ops（运维手册）
位置：.knowledge/ops/{slug}.md
层级：L3（原子能力——SLA / 运维约束）
必填段：TL;DR / 运行参数 / 告警与阈值
质量门禁：运行参数必须有具体数值（超时/重试/并发）

填写指南：
- 数值必须具体，禁止"较短"、"合理"等模糊描述
- 大促保障建议单独拆为一篇 ops（slug 含 daycu 或 holiday 字样）
- 配置变更后必须 --update，并在 sources 追加 Lion key + 时间戳
-->
---
title: {运维主题}                              # 例：打印服务超时机制
type: ops
tags: [{3-5 个标签}]
owner: "@{mis-id}"
created: {YYYY-MM-DD}
expires: {YYYY-MM-DD}                          # 默认 90 天
status: draft
sources:
  - "{Lion 配置 / 监控大盘 / 代码路径}"
related:
  - "[[{关联服务 slug}]]"
---

## TL;DR

{50-100 词，"管什么 + 关键阈值 + 异常时如何兜底"}

## 运行参数

| 参数 | 数值 | 配置位置 | 说明 |
|------|------|---------|------|
| 调用超时 | 30s | `Lion: print.timeout` | 超过即重试 |
| 最大重试 | 3 次 | `Lion: print.retry.max` | 指数退避 |
| 并发上限 | 100 | 代码常量 | 同一商家维度 |
| 限流 QPS | 500 | Rhino | 接口级 |

## 告警与阈值

| 指标 | 告警阈值 | 严重等级 | 通知方式 | 接收人 |
|------|---------|---------|---------|-------|
| 接口错误率 | > 5% (1分钟) | P1 | 大象+电话 | @owner |
| P99 耗时 | > 1000ms (5分钟) | P2 | 大象 | 值班群 |
| 服务可用性 | < 99.9% (1小时) | P1 | 大象+电话 | @owner |

## 大促保障（可选）

| 项 | 大促配置 | 日常配置 |
|----|---------|---------|
| 限流 QPS | 2000 | 500 |
| 实例数 | 30 | 10 |
| 缓存预热 | 提前 2 小时 | — |

- 切换时间：{T-1 日 18:00}
- 回滚时间：{T+1 日 06:00}
- 操作 Owner：{mis-id}

## 故障预案（可选）

### 场景 1：{下游服务不可用}
- 表现：{错误率飙升 + 超时}
- 兜底：{降级到本地缓存 / 切换备用服务}
- 操作步骤：{Lion 改 xxx → 观察 → ...}

### 场景 2：{流量突增}
- 表现：...
- 兜底：...

## 限流配置（可选）

| 维度 | 配置 | 说明 |
|------|------|------|
| 接口级 | 500 QPS | Rhino 全局 |
| 用户级 | 10 QPS | 防刷 |
| 商家级 | 100 QPS | 防异常商家 |

## 部署位置（可选）

- 集群：{cluster-name}
- 机房：{idc-name}
- 实例数：{N}
- 资源：{CPU / 内存}

